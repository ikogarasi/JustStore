import { Box } from "@mui/material";
import Typography from "@mui/material/Typography";

export const NotFound = () => {
  return (
    <Box display="flex" justifyContent="center">
      <Typography variant="h3">Wrong way, bro ;{"("}</Typography>
    </Box>
  );
};
